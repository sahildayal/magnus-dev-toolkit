{
  "_comment": "Optional. Phase 3 applies these ONLY when your global git identity is not already set, and never applies placeholder values. Leave blank to keep whatever git already has.",
  "user": {
    "name": "",
    "email": ""
  }
}
