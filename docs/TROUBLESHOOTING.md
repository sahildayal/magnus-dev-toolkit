# Troubleshooting

- **MCP not loading**: Check `mcp-config.json` for correct node paths.
- **npm install fails**: Run npm command with sudo or check npm paths on Windows.
- **winget issues**: Ensure App Installer is updated in the Microsoft Store.
