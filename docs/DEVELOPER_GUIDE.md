# Developer Guide

To contribute:
1. Clone the repo.
2. Modify the phase scripts in `scripts/phases/`.
3. Test locally using `./scripts/install.ps1 -SkipInteractive`.
4. Modify `mcp-router` inside `mcp-router/` and test using `npm start`.
