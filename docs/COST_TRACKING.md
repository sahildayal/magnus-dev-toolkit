# Cost Tracking

The `budgetTracker.js` module in the `mcp-router` tracks the simulated usage and sends a warning if usage exceeds 90% of the allocated budget.
The budget file is stored in `~/.config/magnus/budget-tracker.json`.
